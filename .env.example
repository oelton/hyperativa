# ==================================
# JWT Configuration
# ==================================

# JWT Secret Key (OBRIGATÓRIO em produção)
# Deve ter no mínimo 256 bits (32 caracteres) para HS256
# Gerar com: openssl rand -base64 32
# NUNCA commitar o valor real no repositório!
JWT_SECRET=your-super-secret-jwt-key-with-at-least-256-bits-long

# JWT Token Expiration (em milissegundos)
# Exemplos:
#   3600000  = 1 hora
#   86400000 = 24 horas (padrão)
#   604800000 = 7 dias
JWT_EXPIRATION=86400000

# JWT Issuer - identifica quem emitiu o token
JWT_ISSUER=crud-api

# JWT Audience - identifica para quem o token foi emitido
JWT_AUDIENCE=crud-api-users

# ==================================
# Database Configuration (Docker)
# ==================================
# Essas configurações são gerenciadas pelo docker-compose.yaml
# Descomente se precisar sobrescrever

# MYSQL_ROOT_PASSWORD=secret
# MYSQL_DATABASE=mydatabase
# MYSQL_USER=myuser
# MYSQL_PASSWORD=secret
